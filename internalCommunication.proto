syntax = "proto3";

service InternalService {
  rpc GetTelemetry(Empty) returns (TelemetryResponse);
}

message Empty {}

message TelemetryResponse {
  double current_latitude = 1;
  double current_longitude = 2;
}
